package main

import "fmt"

// что выведет программа
func main() {
	// использовать буферизированный канал, например: make(chan int, 1)
	intChan := make(chan int)
	intChan <- 1 // 🔴 здесь будет блокировка
	close(intChan)
	fmt.Println(<-intChan)
}

/*
🔍 Почему?

Канал intChan создаётся как небуферизированный канал (make(chan int)).
Операция intChan <- 1 блокируется, потому что:
Небуферизированный канал требует, чтобы приёмник был готов принять значение сразу.
Но приёмника (операции <-intChan) ещё нет.
В результате — main застревает на intChan <- 1, и close(intChan) даже не выполняется.
*/
