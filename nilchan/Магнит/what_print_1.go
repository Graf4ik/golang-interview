package Магнит

import "fmt"

// Какое поведение у кода ниже?
func main() {
	for i := 0; i < 5; i++ {
		go func(n int) {
			fmt.Println(i)
		}(i)
	}
}

/*
🔍 Что происходит:
Переменная i объявлена одна на весь цикл.
Каждая горутина замыкается на одну и ту же переменную i, а не на её копию.
Когда горутина запустится, значение i уже может измениться (или цикл вообще закончиться).
Поэтому в выводе ты можешь увидеть 5, 5, 5, 5, 5 или любое сочетание из [0 1 2 3 4 5].
*/
